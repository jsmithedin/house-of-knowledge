models:
  - nova-lite    # -> app.config.NOVA_LITE_MODEL_ID = "amazon.nova-lite-v1:0"
  - haiku-4.5   # -> app.config.HAIKU_MODEL_ID = "anthropic.claude-haiku-4-5-20251001-v1:0"
                 # NOTE: if .env sets BEDROCK_MODEL_ID to the eu. inference profile
                 # ("eu.anthropic.claude-haiku-4-5-20251001-v1:0"), update HAIKU_MODEL_ID
                 # in app/config.py and re-pin here
k: 5
# Judge model: outside both evaluated families (not Amazon, not Anthropic) to avoid self-preference bias.
# Mistral Large: neutral family, cheapest neutral option in eu-west-2.
# Confirm exact ID (Large 2 vs 3): aws bedrock list-foundation-models --region eu-west-2 --query "modelSummaries[?contains(modelId,'mistral-large')].modelId"
judge_model: mistral.mistral-large-2407-v1:0
faithfulness_threshold: 1.0
